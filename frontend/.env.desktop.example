# Optional: only needed when Rust is installed outside the default directory
# and cargo/rustc are not already available on PATH.
# Copy to .env.desktop.local and use your actual installation paths.
# CARGO_HOME="D:/Tools/Rust/cargo"
# RUSTUP_HOME="D:/Tools/Rust/rustup"
