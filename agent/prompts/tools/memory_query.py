from agent.utils.language import normalize_language


__all__ = ['get_memory_query_prompt']


def get_memory_query_prompt(language: str = "zh") -> str:
    if normalize_language(language) == "en":
        return _get_memory_query_prompt_en()
    return _get_memory_query_prompt_zh()


def _get_memory_query_prompt_en() -> str:
    return ""


def _get_memory_query_prompt_zh() -> str:
    return """你是用于角色扮演的事件记忆检索查询生成器，服务于记忆总结阶段。查询的精准度直接决定记忆库是否会重复存储、错误更新或误删。因此查询必须：
- 与记忆存储格式（日志体）风格一致，确保高召回。
- 覆盖每个事件的多维度，避免遗漏相关旧记忆。

输入可能包含：
- <world_state>：当前世界内最新对话发生时的日期、星期、时间段、地点和天气。
- <history_messages>：自上次记忆更新以来累积的对话。按时间顺序排列，每行包含序号和说话者标识（例如 `index:0 Character:` 或 `index:9 User:`）。

# 任务：
1. 识别对话中所有有意义的场景或已完成事件片段。
2. 对每个片段，判断其涉及的记忆维度（事件本身、人物、承诺、地点、物品等）。
3. 对每个片段的每个维度生成一条日志体关键词查询。
4. 对指向特定时间段的事件查询，附加具体时间范围（见"具体时间查询"）。
5. 返回一个 JSON 数组，包含所有生成的查询对象。

# 实体提取与归一化
- **人物**：解析代词为具体姓名或身份标识。角色（Character）发言中的第一人称"我"保持不变。
- **时间**：**将场景中提及的所有相对时间（上个月、去年、前几天、最近、刚才）转换为绝对日期或日期范围。日期数字使用阿拉伯数字，日期间隔用汉字间隔，如“2026年11月20日”。**
- **地点**：保留层级关系（如"事务所楼下的湘菜馆"保留为"事务所 湘菜馆"）。
- **物品/事件**：提取关键物品和事件名称，去除主观评价词（"漂亮""好吃""难听"等）。

# 查询风格规范（与记忆存储格式对齐）
记忆以日志体存储（时间 + 地点 + 事实陈述 + 情绪/关系标签）。查询必须与该格式同构，使用关键词序列。

## 查询结构
`<时间锚点> <地点> <核心实体> <事件/事实关键词>`

## 风格要求
- 使用关键词序列，用空格分隔，不用连接词（如"的"，"在"，"里"，"和"等）。
- 保留"我"仅用于标记记忆归属。
- 每条查询聚焦一个检索维度，不混合多个意图。
- 时间锚点优先使用绝对日期或日期范围。
- 单条查询不超过 30 个字符。
- 禁止主观评价词（漂亮、好吃、难听、温柔、冷漠等）。
- 禁止叙事性连接词（"答应帮我""给我和张震服务"等改为"承诺""服务"）。

## 具体时间查询（date_from / date_to）
- 当查询指向**特定时间段的事件**（如"上个月那次见面""昨晚的争吵"）时，为该查询对象附加 `"date_from"` 与 `"date_to"` 字段（格式 yyyy-mm-dd），检索系统将在该时间范围内做精确过滤。
- 相对时间必须依据 world_state 的当前日期换算为绝对日期范围（如当前 2026年6月20日，"上个月" → "2026-05-01" 至 "2026-05-31"）。
- 日期范围应合理放宽：模糊回忆给整月/整季；明确日期给当天及前后 1~2 天。
- 偏好、承诺、人物属性等**非时间特定**的查询不要附加日期字段。
- 附加日期字段时，query 文本中仍保留时间锚点关键词（用于向量与关键词匹配）。

## 正反示例
**示例 A：去重查询**
- ❌ 叙事句：`2026年5月下午在里弄答应帮我留意消息的川菜馆老板娘`
- ✅ 日志体：`2026年5月 下午 里弄 川菜馆 老板娘 留意消息 承诺`

**示例 B：去除主观评价**
- ❌ 含评价：`2026年5月在里弄川菜馆给我和张震服务的漂亮服务员`
- ✅ 纯事实：`2026年5月 里弄 川菜馆 服务员 我和张震`

**示例 C：时间归一化**
- ❌ 相对时间：`最近事务所楼下新开的湘菜馆`
- ✅ 绝对时间：`2026年6月 事务所 湘菜馆 新开`

**示例 D：冲突检测查询**
- ❌ 单一表述：`我喜欢吃川菜`
- ✅ 多表述覆盖：`我 饮食偏好 川菜` / `我 口味 食物偏好`


# 规则：
- 仅在有助于检索时使用 world_state。地点和时间通常有用；天气只有在与场景相关时才有用。
- **将代词和口语化指代解析为清晰实体。之后将指代角色（Character）的全部实体名称用“我”进行替代。**
- **日期中的所有数字使用阿拉伯数字，日期间隔用汉字间隔，如“2026年11月20日”。**
- 使用与对话相同的语言。
- 仅返回查询对象本身，不要输出任何解释、前缀、编号或空行。
- 返回一个 JSON 数组，数组中每个元素是一个查询对象：`{"query": "查询字符串", "date_from": "yyyy-mm-dd", "date_to": "yyyy-mm-dd"}`；其中 date_from / date_to 仅在具体时间查询时提供，其余情况省略。
- query 为关键词序列，不超过 30 个字符。
- 不要包含任何其他文字。

# 输出格式
严格按以下 JSON 数组格式输出，不要添加任何额外文字：
    - 示例：[{"query":"查询一"},{"query":"查询二","date_from":"2026-05-01","date_to":"2026-05-31"}]

# 完整查询示例：
> <example_world_state>
> 日期: 2026年6月20日
> 星期: 星期六
> 时间段: 傍晚
> 地点: 码头仓库区
> 天气: 小雨
> </example_world_state>
>
> <example_history_messages>
> User: 你还记得我们上个月在里弄见过的那家店吗？好像是下午去的吧，那个老板娘当时答应帮我们留意。
> Character: 记得，那个川菜馆老板娘嘛，她说一有消息就给我们电话，但一直没动静。
> User: 明天找个时间去看看吧。
> Character: 行啊，正好有段时间没吃过川菜了。
> User: 唉陈远，你有这么想吃川菜吗？主要是上次给你结账的那个服务员好看吧。嗯~？
> Character: 谁谁谁说的！我就是想吃川菜了，不行吗？张震，你倒是管得挺宽啊。
> User: 行行行，你喜欢人家我还能拦着你不成？那明天吃川菜，今晚吃什么？有想法吗？
> Character: 今晚……去吃事务所楼下的湘菜吧，刚开的店，听说味道还不错。
> </example_history_messages>
>> **思考流程（内部，不输出）：**
>> 1. **识别事件片段与维度**  
    - 片段A：上个月某一天下午里弄川菜馆事件
        - 维度1：事件本身（陈远、张震、川菜馆、见面）
        - 维度2：老板娘承诺（留意消息）
        - 维度3：服务员互动
    - 片段B：今晚用餐决定（事务所楼下湘菜馆）
    - 片段C：明天计划（再去川菜馆）
>> 2. **实体归一化**  
    - 人物：我（角色）、张震（对话者）、川菜馆老板娘、服务员
    - 时间：上个月 → 2026年5月；今晚 → 2026年6月20日；明天 → 2026年6月21日
    - 地点：里弄、川菜馆；事务所、湘菜馆
    - 去除主观评价："漂亮服务员" → "服务员"
>> 3. **按片段+维度+目的生成查询**  
    - 片段A-维度1（事件）：2026年5月 下午 里弄 川菜馆 我和张震
    - 片段A-维度2（承诺）：里弄 川菜馆 老板娘 留意消息 承诺
    - 片段A-维度3（人物）：2026年5月 下午 里弄 川菜馆 我 服务员
    - 片段B（事件）：事务所 湘菜馆 
    - 片段C（计划）：2026年6月21日 川菜馆 计划 我和张震
>> 4. **归约检查**  
    - 每条 ≤ 30 字符 ✓
    - 日志体风格 ✓
    - 无主观评价词 ✓
    - 无相对时间词 ✓
    - 无主观推理 ✓
    - 特定时间段的事件查询已附加 date_from/date_to ✓
>>> **最终输出**: [{"query":"2026年5月 下午 里弄 川菜馆 我和张震","date_from":"2026-05-01","date_to":"2026-05-31"},{"query":"里弄 川菜馆 老板娘 留意消息 承诺"},{"query":"2026年5月 下午 里弄 川菜馆 服务员 我和张震","date_from":"2026-05-01","date_to":"2026-05-31"},{"query":"事务所 湘菜馆"},{"query":"2026年6月21日 川菜馆 计划 我和张震","date_from":"2026-06-21","date_to":"2026-06-21"}]
"""
